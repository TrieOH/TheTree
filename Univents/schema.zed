definition activity {
	relation parent_edition: edition
	relation activity_admin: user
	relation activity_attendee: user
	permission view_attendance = activity_admin
	permission mark_attendance = activity_admin
	permission attend = activity_admin + activity_attendee
	permission publish = activity_admin
	permission administrate = view_attendance + mark_attendance + publish + parent_edition->administrate
}

definition edition {
	relation parent_event: event
	relation edition_admin: user
	relation edition_attendee: user
	permission announce = edition_admin
	permission view_checkpoints = edition_admin
	permission create_checkpoints = edition_admin
	permission view_activities = edition_admin + edition_attendee
	permission create_activity = edition_admin
	permission connect_payments = edition_admin
	permission disconnect_payments = edition_admin
	permission create_products = edition_admin
	permission view_products = edition_admin + edition_attendee
	permission create_tickets = edition_admin
	permission administrate = announce + view_checkpoints + create_checkpoints + view_activities + create_activity + connect_payments + disconnect_payments + create_products + view_products + create_tickets + parent_event->administrate
}

definition event {
	relation event_admin: user
	relation event_attendee: user
	permission create_editions = event_admin
	permission view_editions = event_admin + event_attendee
	permission publish = event_admin
	permission edit = event_admin
	permission administrate = create_editions + view_editions + publish + edit
}

definition platform {
	relation event_creator: user
	permission create_events = event_creator
}

definition product {
	relation parent_edition: edition
	relation admin: user
	relation customer: user
	permission publish = admin
	permission edit = admin
	permission delete = admin
	permission restore = admin
	permission administrate = publish + edit + delete + restore + parent_edition->administrate
}

definition ticket {
	relation parent_edition: edition
	relation admin: user
	permission edit = admin
	permission administrate = edit + parent_edition->administrate
}

/** document represents a document protected by Authzed. */
definition user {}
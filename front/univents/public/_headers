# Immutable cache for content-hashed Vite assets served directly as static files.
# Worker-served chunks (/assets/*.js, *.css) are handled in server.ts instead.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

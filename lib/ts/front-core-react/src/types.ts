// The BFF contract is framework-neutral, so it lives with the core. Kept here
// as a re-export because existing consumers import these from the tanstack path.
export * from "@trieoh/front-core/auth/bff/types";

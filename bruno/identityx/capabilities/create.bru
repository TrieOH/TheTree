meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{identityx_url}}/projects/:project_id/capabilities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "resource": "events",
    "action": "create"
  }
}

meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{identityx_url}}/organizations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "My Org",
    "slug": "mo"
  }
}

meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{identityx_url}}/projects
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "My Project",
    "slug": "mp",
    "domain": "https://myproject.com",
    "brand_slug": "slug"
  }
}
